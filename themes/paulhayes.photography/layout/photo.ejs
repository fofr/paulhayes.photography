<%
  photo = page;
  photo.filename = photo.filename || photo.slug;
  photo.file = config.image_dir + '1720/' + photo.filename + '.' + (photo.type ? photo.type : 'jpg');
%>
<main role="main">
  <article class="container single-photo clearfix" id="photo">
    <div class="photo-image-wrapper">
      <img src="<%= photo.file %>" class="photo-image" alt="<%= photo.title %>" />
      <%- partial('_partial/post/nav', {post: photo}) %>
    </div>
    <div class="photo-detail">
      <header class="photo-header">
        <h1 class="main-content-title photo-title">
          <%= photo.title %>
        </h1>
      </header>
      <div class="photo-content">
        <%- photo.content %>
      </div>
    </div>
    <div class="photo-meta">
      <%- partial('_partial/photo/exif', {photo: photo}) %>
      <%- partial('_partial/post/published', {post: photo}) %>
      <%- partial('_partial/photo/external', {photo: photo}) %>

    </div>
  </article>
</main>
